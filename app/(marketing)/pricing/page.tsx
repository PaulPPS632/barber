import { PricingCard } from "@/components/ui/pricing-card";

export const metadata = { title: "Planes y Precios — Barber.pe" };

export default function PricingPage() {
  return (
    <div className="pt-24">
      <PricingCard />
    </div>
  );
}
